import { OverUnderPipe } from './over-under.pipe';

describe('OverUnderPipe', () => {
  it('create an instance', () => {
    const pipe = new OverUnderPipe();
    expect(pipe).toBeTruthy();
  });
});
